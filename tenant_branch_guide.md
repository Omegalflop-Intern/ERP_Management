# ৩৩টি কোর মডিউলে টিন্যান্ট ও ব্রাঞ্চ ম্যাপিং গাইড

এই ডকুমেন্টে সুপার অ্যাডমিন (Super Admin) মডিউলটি সম্পূর্ণ বাদ দিয়ে, বাকি **৩৩টি মডিউলের** জন্য টিন্যান্ট (Tenant) এবং ব্রাঞ্চ (Branch) স্তরের যাবতীয় কনফিগারেশন, স্কোপ এবং ডাটাবেজ ফিল্ডের একটি পূর্ণাঙ্গ বিবরণ দেওয়া হয়েছে। সিঙ্গেল-শপ (Single-shop) প্রজেক্টে কনভার্ট বা রিফ্যাক্টর করার জন্য এটি একটি নির্দেশিকা হিসেবে কাজ করবে।

---

## মডিউল তালিকা ও ডাটাবেজ স্কোপিং বিবরণ

### ১. `accounting` (হিসাববিজ্ঞান ও জার্নাল এন্ট্রি)
* **Tenant Scope:** প্রতিটি টিন্যান্টের জন্য নিজস্ব চার্ট অব অ্যাকাউন্টস (CoA) তৈরি করা থাকে। অ্যাকাউন্টের ব্যালেন্স এবং রিপোর্ট জেনারেশন টিন্যান্ট লেভেলে ডেটাবেজে সম্পূর্ণ আইসোলেটেড।
* **Branch Scope:** প্রতিটি জার্নাল এন্ট্রি লাইনে `branch_id` কলাম থাকে, যার ফলে আউটলেট ভিত্তিক ট্রায়াল ব্যালেন্স এবং পিঅ্যান্ডএল (P&L) স্টেটমেন্ট জেনারেট করা যায়।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২. `attendance` (কর্মচারী হাজিরা)
* **Tenant Scope:** টিন্যান্ট লেভেলে কর্মচারীদের জন্য ডিফল্ট হাজিরা পলিসি (লেট চেক-ইন বা ওভারটাইম লজিক) ও ক্যালকুলেশন প্রযোজ্য হয়।
* **Branch Scope:** কর্মচারীরা নির্দিষ্ট কোন আউটলেটে চেক-ইন বা চেক-আউট করছে তা `branch_id` দিয়ে ট্র্যাক করা হয়।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ৩. `audit` (অ্যাক্টিভিটি লগ)
* **Tenant Scope:** প্রতিটি টিন্যান্টের ইউজার অ্যাক্টিভিটি হিস্ট্রি আলাদা অডিট ট্রেইলে সংরক্ষিত থাকে।
* **Branch Scope:** কোন অ্যাকশনটি কোন ব্রাঞ্চের আইডি বা আইপি থেকে পারফর্ম করা হয়েছে তা ট্র্যাক করে।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ৪. `auth` (অথেনটিকেশন ও টোকেন)
* **Tenant Scope:** রিকোয়েস্ট ডোমেন/সাবডোমেন সনাক্ত করে সঠিক টিন্যান্ট ডাটাবেজ স্কোপে ব্যবহারকারীকে ভেরিফাই করা।
* **Branch Scope:** লগইন সেশন বা JWT টোকেনের পে-লোডে ব্যবহারকারীর অ্যাক্সেসযোগ্য `branchId` এম্বেড করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ৫. `branch` (শাখা/আউটলেট সেটিংস)
* **Tenant Scope:** প্রতিটি ব্রাঞ্চ একটি নির্দিষ্ট টিন্যান্ট আইডির অধীনে সংযুক্ত থাকে।
* **Branch Scope:** আউটলেটের নাম, ঠিকানা, ইমেইল, এবং ফোন নাম্বার কনফিগার করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### 6. `catalog` (ক্যাটাগরি ও ব্র্যান্ড)
* **Tenant Scope:** পণ্যের ক্যাটাগরি এবং ব্র্যান্ডের তালিকা টিন্যান্ট অনুযায়ী আলাদা করা।
* **Branch Scope:** নেই (সব ব্রাঞ্চে একই ক্যাটালগ এভেইলেবল থাকে)।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ৭. `contact` (যোগাযোগ মেসেজ)
* **Tenant Scope:** টিন্যান্টের পাবলিক কন্ট্যাক্ট ফর্ম থেকে সাবমিট করা মেসেজ টিন্যান্টের কাস্টমার প্যানেলে আইসোলেট করা।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ৮. `customer` (গ্রাহক ডাটা)
* **Tenant Scope:** কাস্টমারের ফোন নাম্বার, বকেয়া হিসাব এবং পারচেজ হিস্ট্রি টিন্যান্ট স্কোপে ডেটা আইসোলেটেড থাকে।
* **Branch Scope:** নেই (যাতে কাস্টমার যেকোনো আউটলেটে গেলে তার বকেয়া সিঙ্ক থাকে)।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ৯. `documentVault` (ফাইল ভল্ট)
* **Tenant Scope:** ফাইল ও ডকুমেন্ট ডাউনলোডের অ্যাক্সেস টিন্যান্টের মালিকানাধীন ফোল্ডারে সীমাবদ্ধ থাকে।
* **Branch Scope:** আপলোড করা ডকুমেন্টে ঐচ্ছিক `branch_id` ম্যাপ করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১০. `employee` (এইচআরএম কর্মচারী)
* **Tenant Scope:** কর্মচারীদের বেতন কাঠামো, পার্সোনাল ডাটা টিন্যান্টের অধীনে সম্পূর্ণ আইসোলেটেড।
* **Branch Scope:** কর্মচারী নির্দিষ্ট কোন শাখার অধীনে কর্মরত তা ট্র্যাক করা হয়।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১১. `expense` (খরচ ট্র্যাকিং)
* **Tenant Scope:** ক্যাশ balance থেকে খরচের এন্ট্রি ডিডাক্ট করা।
* **Branch Scope:** ব্যয় বা ইউটিলিটি বিলটি কোন আউটলেটের নামে এন্ট্রি হচ্ছে তার জন্য `branch_id` ব্যবহৃত হয়।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১২. `imei` (মোবাইল আইএমইআই ডাটা)
* **Tenant Scope:** IMEI স্টক আইটেমগুলো টিন্যান্ট লেভেলে ডেটাবেজে ভেরিফাই হয়।
* **Branch Scope:** প্রতিটি ইউনিক সিরিয়াল বা IMEI নম্বরটি বর্তমানে কোন আউটলেটের স্টক শেলফ বা ব্রাঞ্চ স্টকে আছে তা নিশ্চিত করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১৩. `investor` (বিনিয়োগকারী ইকুইটি)
* **Tenant Scope:** ইনভেস্টরদের প্রোফাইল, মূলধন জমা ও প্রফিট শেয়ার টিন্যান্ট আইডির অধীনে কাজ করে।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ১৪. `leave` (ছুটি ম্যানেজমেন্ট)
* **Tenant Scope:** লিভ টাইপ ও নিয়মাবলী টিন্যান্ট স্তরে সেটআপ করা থাকে।
* **Branch Scope:** কোনো ব্রাঞ্চের ম্যানেজার তার শাখার কর্মচারীদের ছুটির রিকোয়েস্ট ফিল্টার ও অ্যাপ্রুভ করতে পারেন।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১৫. `loan` (ঋণ ও কিস্তি)
* **Tenant Scope:** ব্যাংক লোন বা লাইবিলিটি এবং ডাবল-এন্ট্রি বুককিপিং হিসাব টিন্যান্টের CoA-তে বুকড হয়।
* **Branch Scope:** কোন শাখার পেমেন্ট অ্যাকাউন্ট থেকে কিস্তির এন্ট্রি করা হলো তা ট্র্যাকিং।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১৬. `notification` (সিস্টেম নোটিফিকেশন)
* **Tenant Scope:** system নোটিফিকেশনগুলো টিন্যান্টের নির্দিষ্ট ইউজারদের পুশ করা হয়।
* **Branch Scope:** নির্দিষ্ট ব্রাঞ্চ টার্গেট করে নোটিফিকেশন পাঠানো।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১৭. `payroll` (পে-রোল শীট)
* **Tenant Scope:** কর্মচারীদের পে-রোল হিসাব প্রসেস এবং ব্যাংকের মাধ্যমে বেতন ডিসবার্সমেন্ট ট্র্যাক করা।
* **Branch Scope:** আউটলেট ভিত্তিক পে-রোল রেভিনিউ আলাদা করার জন্য `branch_id` ম্যাপ থাকে।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ১৮. `plans` (সাবক্রিপশন প্ল্যান)
* **Tenant Scope:** টিন্যান্টের বিলিং সাইকেল নির্ধারণ ও চেকআউট ম্যানেজমেন্ট।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ১৯. `product` (পণ্য তালিকা)
* **Tenant Scope:** প্রোডাক্ট ডেফিনিশন, নাম, ইমেজ এবং এসকেইউ (SKU) টিন্যান্ট লেভেলে রেজিস্টার হয়।
* **Branch Scope:** নেই (সব ব্রাঞ্চে শেয়ারড প্রোডাক্ট ক্যাটালগ থাকে)।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ২০. `purchase` (ক্রয় অর্ডার)
* **Tenant Feature:** সাপ্লাইয়ার পারচেজ ইনভয়েস ভ্যালু টিন্যান্ট লেভেলের অ্যাকাউন্টিং লাইবিলিটিতে হিট করে।
* **Branch Feature:** পারচেজ করা প্রোডাক্টের ফিজিক্যাল স্টক কোন শাখার ইনভেন্টরিতে ঢুকছে তার ট্র্যাকিং।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২১. `repair` (মোবাইল রিপেয়ার টিকেট)
* **Tenant Scope:** রিপেয়ার বিলিং রেভিনিউ ট্র্যাক করা।
* **Branch Scope:** কাস্টমারের ডিভাইসটি সার্ভিসিং এর জন্য কোন ব্রাঞ্চ আউটলেটে রিসিভ করা হয়েছে এবং কোথা থেকে ডেলিভারি দেওয়া হচ্ছে তা নিয়ন্ত্রণ করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২২. `report` (রিপোর্ট ও অ্যানালিটিক্স)
* **Tenant Scope:** টিন্যান্ট কোম্পানির সামগ্রিক ফিন্যান্সিয়াল লাভ-ক্ষতির অডিট রিপোর্ট।
* **Branch Scope:** ফিল্টারিং অপশনে `branchId` ব্যবহার করে নির্দিষ্ট আউটলেটের পারফরম্যান্স ও সেলস অ্যানালিটিক্স দেখা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২৩. `role` (পারমিশন রোলস)
* **Tenant Scope:** রোলস এবং তার অধীনে পারমিশন স্কোপগুলো টিন্যান্ট লেভেলে আইসোলেটেড।
* **Branch Scope:** নির্দিষ্ট আউটলেট রোলের অ্যাক্সেস কন্ট্রোল।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ২৪. `sale` (POS বিক্রয়)
* **Tenant Scope:** সামগ্রিক সেলস জার্নাল এন্ট্রি।
* **Branch Scope:** কোন ব্রাঞ্চের কাউন্টার থেকে পণ্যটি সেল করা হচ্ছে এবং কার সোল্ড আইটেম ক্যাশ ট্র্যাকে যাবে তা `branch_id` দিয়ে চিহ্নিত করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২৫. `settings` (শপ গ্লোবাল সেটিংস)
* **Tenant Scope:** শপের স্লোগান, লোগো, সেটিংস ও চালান ফরম্যাট টিন্যান্ট লেভেলে ডেভলপ করা।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ২৬. `sse` (রিয়েল-টাইম লাইভ ইভেন্ট)
* **Tenant Scope:** কানেকশনগুলো টিন্যান্ট আইডি দিয়ে চ্যানেল ফিল্টার করা হয় যাতে ডাটা সিকিউরড থাকে।
* **Branch Scope:** কোন ব্রাঞ্চের POS ক্যাশ কাউন্টারে ডাটা সিঙ্ক হচ্ছে তা ম্যাপ করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ২৭. `stock` (ইনভেন্টরি ও ব্রাঞ্চ ট্রান্সফার)
* **Tenant Scope:** টিন্যান্টের মোট স্টক ইনভেন্টরি কস্ট অব গুডস সোল্ড (COGS) ক্যালকুলেশন।
* **Branch Scope:** 
  * `product_branch_stocks` টেবিলের মাধ্যমে আলাদা আউটলেটের স্টক ট্র্যাক করা।
  * স্টক এক শাখা থেকে অন্য শাখায় স্থানান্তর করার রিলেশনশিপ।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ২৮. `supplier` (সরবরাহকারী ডাটা)
* **Tenant Scope:** সাপ্লাইয়ারের কন্ট্যাক্ট এবং বকেয়া (Payable Balance) টিন্যান্ট লেভেলে সেভড থাকে।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `tenant_id` (INT)

### ২৯. `tenant` (টিন্যান্ট প্রোফাইল)
* **Tenant Scope:** শপের নাম, ডোমেন সেটিংস, মালিকের নাম এবং বিলিং স্ট্যাটাস।
* **Branch Scope:** নেই।
* **ডাটাবেজ কলাম:** `id` (Primary Key)

### ৩০. `ticket` (কমপ্লেইন টিকিট)
* **Tenant Scope:** টিকিট তালিকা টিন্যান্ট অনুযায়ী পৃথক থাকে।
* **Branch Scope:** কোনো নির্দিষ্ট ব্রাঞ্চের কাস্টমার সাপোর্ট কেস ও টিকেট স্ট্যাটাস ট্র্যাক করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ৩১. `user` (সিস্টেম ইউজার)
* **Tenant Scope:** টিম মেম্বারদের অ্যাকাউন্ট ডাটা এবং অথেনটিকেশন আইসোলেশন।
* **Branch Scope:** ব্যবহারকারী কোনো নির্দিষ্ট শাখার দায়িত্বে নিয়োজিত স্টাফ কি না তার জন্য `branch_id` ট্র্যাকিং।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### 3২. `warranty` (ওয়ারেন্টি ম্যানেজমেন্ট)
* **Tenant Scope:** কাস্টমার ও সাপ্লাইয়ার ওয়ারেন্টি ক্লেইম বুকিং।
* **Branch Scope:** কোন ব্রাঞ্চের মাধ্যমে ডিভাইস ওয়ারেন্টি রিসিভ ও সাবমিট হলো তা ম্যাপ করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

### ৩৩. `wholesale` (পাইকারি বিক্রয়)
* **Tenant Scope:** ডিলারের বাল্ক ডিসকাউন্ট পলিসি ও পাইকারি পেমেন্ট ট্র্যাক করা।
* **Branch Scope:** কোন শাখা থেকে পাইকারি ডেলিভারিটি প্রসেস ও বিল করা হলো তা ট্র্যাক করা।
* **ডাটাবেজ কলাম:** `tenant_id` (INT), `branch_id` (INT)

---

## 🛠️ সিঙ্গেল-শপ কোডবেসে রিফ্যাক্টরিং গাইডলাইন

আপনার অন্য প্রজেক্টটি যদি **সিঙ্গেল শপ** হয়, তবে ডাটাবেজ এবং সোর্স কোড মডিফাই করার জন্য নিচের স্টেপগুলো ফলো করুন:

1. **ডাটাবেজ কলাম রিমুভাল:**
   * সমস্ত টেবিল থেকে `tenant_id` কলামটি ড্রপ করে দিন। 
   * `tenants` এবং `plans` টেবিল দুটির আর কোনো প্রয়োজন নেই, এগুলো সম্পূর্ণ বাদ দিতে পারেন।

2. **কোয়েরি ফিল্টার আপডেট:**
   * Knex কোয়েরিতে যেখানে `applyTenantScope(query, tenantId)` কল করা আছে, সেগুলো রিমুভ করুন।
   * জয়েন কুয়েরি থেকে টিন্যান্ট ফিল্টারিং বা `where('tenant_id', tenantId)` কন্ডিশনগুলো কেটে ফেলুন।

3. **মিডলওয়্যার নিষ্ক্রিয় করা:**
   * `server/middleware/subdomain.middleware.js` ফাইলটি বাদ দিন এবং `app.js` থেকে এর ইনজেকশন বন্ধ করুন। 

4. **ইউনিক কী মডিফিকেশন:**
   * সাবডোমেন বা টিন্যান্ট আইডি সহ যে কম্পোজিট ইউনিক কী তৈরি করা ছিল (যেমন: `UNIQUE KEY unique_product (tenant_id, sku)`), সেগুলোকে সরাসরি কলামের অধীনে ইউনিক করুন (যেমন: `UNIQUE KEY unique_product (sku)`).
